/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/venje-realty',
  assetPrefix: '/venje-realty/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
