import AdminDashboard from "@/app/components/admin/AdminDashboard";

export const metadata = { title: "Admin Dashboard | Venje Realty" };

export default function AdminPage() {
  return <AdminDashboard />;
}
